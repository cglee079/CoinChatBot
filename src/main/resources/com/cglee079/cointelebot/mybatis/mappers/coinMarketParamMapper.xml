<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cglee079.cointelebot.mapper.CoinMarketParamMapper">

	<select id="get" resultType="map">
			SELECT * 
			FROM TB_COINBOT_MARKET_PARAM
			WHERE 1 = 1
			AND MARKET = #{market}
	</select>
	
</mapper>